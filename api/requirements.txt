# Core API — dùng >= để tương thích với starlette/pydantic mà scanner tools (checkov, semgrep) cài thêm
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
# DB — giữ nguyên để tránh migration issues
sqlalchemy==2.0.30
alembic==1.13.1
# Pydantic — dùng >= để pip tự resolve khi scanner tools upgrade
pydantic>=2.7.1
pydantic-settings>=2.2.1
# Utilities
httpx>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.1
python-dateutil>=2.9.0
rich>=13.7.1
typer>=0.12.3
